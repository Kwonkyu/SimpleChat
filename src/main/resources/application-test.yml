spring:
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
logging:
  level:
    org:
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql: trace
      springframework:
        web: debug